services:
  api:
    build: ./api
    volumes:
      - ./api/data:/app/data 
  ui:
    build: ./ui
    ports:
      - "3000:3000"
    volumes:
      - ./ui:/app

    stdin_open: true
    tty: true
    

